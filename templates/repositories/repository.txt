const BaseRepository = require('fun.framework/classes/src/BaseRepository');

const {{ resourceCaps }} = require('../models/{{ resourceCaps }}');

class {{ resourceCaps }}Repository extends BaseRepository {
  constructor() {
    super({{ resourceCaps }});
  }
}

module.exports = {{ resourceCaps }}Repository;
