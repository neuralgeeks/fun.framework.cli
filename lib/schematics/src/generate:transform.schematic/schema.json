{
  "$schema": "http://json-schema.org/schema",
  "id": "fun-cli.generate:transform",
  "title": "Generate transform",
  "type": "object",
  "description": "Generates a new transform inside a fun.framework project",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the transform",
      "x-prompt": "Enter the name of the new transform"
    },
    "service": {
      "type": "string",
      "description": "The name of the service that will contain the transform",
      "x-prompt": "Enter the name of the service that will contain the transform"
    }
  },
  "required": ["name", "service"]
}
