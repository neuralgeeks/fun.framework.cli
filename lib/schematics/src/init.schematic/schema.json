{
  "$schema": "http://json-schema.org/schema",
  "id": "fun-cli.init",
  "title": "init",
  "type": "object",
  "description": "Inits a fun.framework project",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the new projects",
      "x-prompt": "How would you want to name the project?",
      "default": "project"
    },
    "shouldGenerateService": {
      "type": "boolean",
      "description": "Wether or not the init command should create de base API.service",
      "x-prompt": "Would you like to create a base API service?",
      "default": true
    },
    "version": {
      "type": "string",
      "description": "The version of fun-cli"
    }
  },
  "required": ["name", "shouldGenerateService", "version"]
}
