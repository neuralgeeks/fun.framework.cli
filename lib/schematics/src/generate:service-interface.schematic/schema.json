{
  "$schema": "http://json-schema.org/schema",
  "id": "fun-cli.generate:service-interface",
  "title": "Generate service interface",
  "type": "object",
  "description": "Generates a new service interface inside a fun.framework service",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the new service interface",
      "x-prompt": "Enter the name of the new service interface",
      "minLength": 1,
      "pattern": "[A-Za-z0-9][^]*[A-Za-z0-9]"
    }
  },
  "required": ["name"]
}
