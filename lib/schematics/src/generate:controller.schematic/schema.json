{
  "$schema": "http://json-schema.org/schema",
  "id": "fun-cli.generate:controller",
  "title": "Generate controller",
  "type": "object",
  "description": "Generates a new controller inside a fun.framework project",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the controller",
      "x-prompt": "Enter the name of the new controller"
    },
    "service": {
      "type": "string",
      "description": "The name of the service that will contain the controller",
      "x-prompt": "Enter the name of the service that will contain the controller"
    }
  },
  "required": ["name", "service"]
}
