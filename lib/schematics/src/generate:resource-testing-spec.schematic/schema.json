{
  "$schema": "http://json-schema.org/schema",
  "id": "fun-cli.generate:resource-testing-spec",
  "title": "Generate resource testing spec",
  "type": "object",
  "description": "Generates the testing specs of a given resource",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the resource",
      "x-prompt": "Enter the name of the resource"
    },
    "service": {
      "type": "string",
      "description": "The name of the service that will contain the testing specs",
      "x-prompt": "Enter the name of the service that will contain the testing specs"
    }
  },
  "required": ["name", "service"]
}
