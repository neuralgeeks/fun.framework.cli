{
  "$schema": "http://json-schema.org/schema",
  "id": "fun-cli.generate:repository",
  "title": "Generate repository",
  "type": "object",
  "description": "Generates a new repository inside a fun.framework project",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the repository",
      "x-prompt": "Enter the name of the new repository"
    },
    "service": {
      "type": "string",
      "description": "The name of the service that will contain the repository",
      "x-prompt": "Enter the name of the service that will contain the repository"
    }
  },
  "required": ["name", "service"]
}
