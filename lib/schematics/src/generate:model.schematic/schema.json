{
  "$schema": "http://json-schema.org/schema",
  "id": "fun-cli.generate:model",
  "title": "Generate model",
  "type": "object",
  "description": "Generates a new model inside a fun.framework project",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the model",
      "x-prompt": "Enter the name of the new model"
    },
    "service": {
      "type": "string",
      "description": "The name of the service that will contain the model",
      "x-prompt": "Enter the name of the service that will contain the model"
    }
  },
  "required": ["name", "service"]
}
