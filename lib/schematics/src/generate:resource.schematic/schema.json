{
  "$schema": "http://json-schema.org/schema",
  "id": "fun-cli.generate:resource",
  "title": "Generate reseource",
  "type": "object",
  "description": "Generates a new resource inside a fun.framework project",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the resource",
      "x-prompt": "Enter the name of the new resource",
      "default": "entity"
    }
  },
  "required": ["name"]
}
