{
  "$schema": "http://json-schema.org/schema",
  "id": "fun-cli.generate:routes",
  "title": "Generate routes",
  "type": "object",
  "description": "Generates a new routes inside a fun.framework project",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the routes",
      "x-prompt": "Enter the name of the new routes"
    },
    "service": {
      "type": "string",
      "description": "The name of the service that will contain the routes",
      "x-prompt": "Enter the name of the service that will contain the routes"
    }
  },
  "required": ["name", "service"]
}
